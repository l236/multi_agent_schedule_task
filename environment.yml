name: multi-agent-scheduler
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - python-dotenv>=1.0.0
  - pyyaml>=6.0
  - fastapi>=0.100.0
  - uvicorn>=0.20.0
  - pydantic>=2.0.0
  - pip:
    - langchain>=0.2.0
    - autogen>=0.8.0
    - -e .
